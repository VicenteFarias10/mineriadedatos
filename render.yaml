services:
  - type: web
    name: mineria-datos-climaticos
    env: python
    plan: starter
    buildCommand: |
      pip install -r requirements.txt
      python setup_models.py
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0 